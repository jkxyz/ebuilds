name: Update packages

on:
  schedule:
    - cron: "17 4 * * *"
  workflow_dispatch:

concurrency:
  group: update-packages
  cancel-in-progress: false

permissions: {}

jobs:
  discover:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    defaults:
      run:
        shell: bash
    permissions:
      contents: read
    outputs:
      result: ${{ steps.packages.outputs.result }}
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Discover package updates
        id: packages
        run: echo "result=$(python scripts/updates_matrix.py)" >> "$GITHUB_OUTPUT"

  update:
    needs: discover
    if: >-
      needs.discover.result == 'success' &&
      fromJSON(needs.discover.outputs.result).has_updates
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.discover.outputs.result).matrix }}
    defaults:
      run:
        shell: bash
    permissions:
      contents: write
      packages: read
      pull-requests: write
    container:
      image: ghcr.io/jkxyz/ebuilds-gentoo-tools:latest
      credentials:
        username: ${{ github.actor }}
        password: ${{ github.token }}
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Configure Git safe directory
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Bump package and regenerate Manifest
        id: release
        env:
          ATOM: ${{ matrix.atom }}
          VERSION: ${{ matrix.version }}
        run: scripts/bump_packages.py "$ATOM" "$VERSION" >> "$GITHUB_OUTPUT"

      - name: Run package QA on the generated ebuild
        if: steps.release.outputs.updated == 'true'
        env:
          ATOM: ${{ matrix.atom }}
          EBUILD: ${{ steps.release.outputs.ebuild }}
        run: pkgcheck scan --exit GentooCI --cache-dir /tmp/pkgcheck "${ATOM}/${EBUILD}"

      - name: Open update pull request
        if: steps.release.outputs.updated == 'true'
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          token: ${{ github.token }}
          base: main
          branch: automation/${{ matrix.slug }}-update
          delete-branch: true
          sign-commits: true
          draft: false
          add-paths: |
            ${{ matrix.atom }}
          commit-message: >-
            ${{ matrix.atom }}: bump to ${{ steps.release.outputs.version }}
          title: >-
            ${{ matrix.atom }}: bump to ${{ steps.release.outputs.version }}
          body: |
            Updates `${{ matrix.atom }}` from `${{ steps.release.outputs.previous_version }}` to `${{ steps.release.outputs.version }}`.

            The release was discovered by the package's stable version probe. `pkgbump` created the testing ebuild and regenerated its Manifest, then `pkgcheck` scanned the generated ebuild.

            Generated by the `Update packages` workflow.
